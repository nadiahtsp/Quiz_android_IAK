<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nadiah.quiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".quiz1" />
        <activity android:name=".quiz2" />
        <activity android:name=".pembahasan1" />
        <activity android:name=".pembahasan2" />
        <activity android:name=".quiz3" />
        <activity android:name=".pembahasan3" />
        <activity android:name=".quiz4" />
        <activity android:name=".pembahasan4" />
        <activity android:name=".quiz5" />
        <activity android:name=".pembahasan5"></activity>
    </application>

</manifest>